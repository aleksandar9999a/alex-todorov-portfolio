export interface IStarCoords {
  x: number,
  y: number,
  vx: number;
  vy: number;
  radius: number;
}

export interface ISocialLink {
  id: string | number,
  name: string,
  image: string,
  link: string
}